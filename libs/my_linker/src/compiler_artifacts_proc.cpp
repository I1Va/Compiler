#include <assert.h>

#include "compiler_artifact_structs.hpp"


// void merge_symbol_sections(symbol_section_t *src, symbol_section_t *dst) {
//     assert(dst);
//     assert(src);
//     assert(src->syms_section_sz + dst->syms_section_sz < MAX_SYMBOL_SECTION_SZ);

//     for (size_t i = 0; i , )
//     for (size_t i = 0; i < src->syms_section_sz; i++) {
//         dst[dst->syms_section_sz + i] =
//     }

// }
// void link_object_files_into_executable() {

// }

